---
export interface Props {
  id?: string
  as?: "section" | "div"
  class?: string
}

const { id, as: Tag = "section", class: className } = Astro.props
---

<Tag id={id} class:list={["section", className]}>
  <slot />
</Tag>

<style>
  .section {
    padding-block: var(--space-16);
  }
</style>
